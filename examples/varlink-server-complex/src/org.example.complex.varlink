interface org.example.complex

type TypeEnum ( enum, b, c )

type TypeFoo (
    bool: bool,
    int: int,
    float: float,
    string: string,
    enum: ( foo, bar, baz )[],
    type: TypeEnum,
    anon: ( foo: bool, bar: int, baz: (a: int, b: int)[] )
)

method Foo(enum: (b: bool, c: int), foo: TypeFoo) -> (a: (b: bool, c: int)[], foo: TypeFoo)
method Bar() -> ()

error ErrorFoo (enum: (b: bool, c: int), foo: TypeFoo, bar: (type, enum, int, bool, string, if, let))
error ErrorBar ()
